-Fix indentation still messes up some continued lines(fix_missing_newline:22)
-Check that every .changed() first checks if we're actually changing
-Check for non-unicode strings
-Test it against own codebase and pep8 code after
-Get README/Documentation setup
-Make public
-TravisCI


Issues
-pep8 complains about maximum line length out line 52

-add to maximum_line_length
    def testing_func():
        if (node.type == symbols.funcdef and node.parent.type != symbols.decorated and node.type == symbols.decorated
            or node.type == symbols.classdef or node.type == symbols.decorated or node.type == symbols.simple_stmt):
            return True
-allow to break on string during maximum_line_length